import Logo from "../Logo";

const Footer = () => {
  return (
    <footer className="mt-auto py-1.5 bg-primary opacity-70 flex justify-center items-center sticky bottom-0 z-50">
      <Logo className="h-[32px]"/>
    </footer>
  );
};

export default Footer;
