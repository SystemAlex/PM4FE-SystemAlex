import FromContent from "@/components/FormContent";
import LoginComponent from "../../components/LoginComponent";

const Login = () => {
  return <FromContent title="LogIn" formComponent={<LoginComponent />} />;
};

export default Login;
