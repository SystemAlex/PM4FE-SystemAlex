import FromContent from "@/components/FormContent";
import RegisterComponent from "@/components/RegisterComponent";

const Register = () => {
  return <FromContent title="SingUp" formComponent={<RegisterComponent />} />;
};

export default Register;
